

const initialState = {
    pokemons: [],
    types: [],
    currentPokemon: {},
    currentPage: 1
}

function Reducer(state = initialState, action) {
    switch (action.type) {

    }
}

export default Reducer;